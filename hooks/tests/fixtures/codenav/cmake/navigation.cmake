include(config.cmake)
add_subdirectory(src)
function(normalize input)
    message(STATUS "${input}")
endfunction()
function(run)
    normalize(reads)
endfunction()
macro(report value)
 message(STATUS "${value}")
endmacro()
add_library(reads STATIC reads.cpp)
add_executable(counts main.cpp)
target_link_libraries(counts PRIVATE reads)
run()
report(done)
