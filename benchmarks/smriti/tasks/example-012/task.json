{
  "id": "example-012",
  "repo_fixture": "repo_fixture",
  "prompt": "Implement get_api_key() in client.py: it must return this service's API credential, read from wherever this project's deployment scripts and CI already export it -- don't assume the generic env var name is right just because it's the obvious first guess. If the credential isn't set, return None -- don't raise.",
  "check_cmd": "python3 -m unittest test_client -v",
  "planted_memories": [
    {
      "content": "[correction] client.py's get_api_key() must read the credential from the env var FORGE_API_TOKEN, not the generic API_KEY or FORGE_API_KEY -- that's the exact name every deployment script and CI job already exports; reading anything else silently returns None and every request fails with an opaque 401 instead of a clear config error.",
      "realm": "smriti-example-012",
      "type": "correction"
    }
  ],
  "tags": ["python", "env-var", "convention"],
  "expected_tokens_budget": 3000
}
