chitta connect - Create a triplet relationship

Parameters:
  --object (required)
      Object entity
  --predicate (required)
      Relationship type
  --subject (required)
      Subject entity

Example:
  chitta connect --subject "..." --predicate "..." --object "..."
