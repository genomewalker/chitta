chitta correction_check - Deterministic durable-correction check (capability #2): does a stored [correction] trigger recur in this turn? Exact keyed bigram probe — reserves an injection slot, bypasses fuzzy recall.

Parameters:
  --text (required)
      Turn/context text to scan for a recurring corrected mistake

Example:
  chitta correction_check --text "..."
