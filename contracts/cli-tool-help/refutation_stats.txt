chitta refutation_stats - Show Sequitur rules that are being falsified: rules whose antecedent appears but is NOT followed by the expected consequent. Returns live/refuted counts and top-k by refutation ratio.

Parameters:
  --k
      Max rules to show (default 10)

Example:
  chitta refutation_stats
