chitta lookup - Unified memory lookup. Classifies intent, fans out to keyword/semantic/triplet/temporal/code backends, fuses with weighted RRF. Use this as the default memory search.

Parameters:
  --explain

  --limit

  --mode

  --query (required)

  --realm


Example:
  chitta lookup --query "..."
