chitta realm_visibility - Set realm visibility for a memory

Parameters:
  --id (required)
      Memory ID
  --visibility (required)
      Visibility: private|shared|global

Example:
  chitta realm_visibility --id "..." --visibility "..."
