chitta recall_spreading - Retrieve memories via entity graph spreading activation

Parameters:
  --limit
      Max results (default 10)
  --query (required)
      Query; entity seeds extracted automatically
  --realm
      Realm filter (optional)

Example:
  chitta recall_spreading --query "..."
