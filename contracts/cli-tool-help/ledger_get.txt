chitta ledger_get - Get checkpoint by key or session/project

Parameters:
  --include_snapshot
      Include snapshot preview (truncated) in response
  --key
      Canonical checkpoint key (session:project)
  --project

  --session_id


Example:
  chitta ledger_get
