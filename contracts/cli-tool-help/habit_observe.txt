chitta habit_observe - Record a trigger→response pattern

Parameters:
  --trigger (required)
      What triggers the habit
  --response (required)
      What should happen when triggered
  --realm
      Realm

Example:
  chitta habit_observe --trigger "..." --response "..."
