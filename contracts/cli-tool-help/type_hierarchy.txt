chitta type_hierarchy - Get type hierarchy (base classes, implemented interfaces) for a type

Parameters:
  --name (required)
      Type name to query
  --direction [default: both]
      ancestors, descendants, or both (default: both)

Example:
  chitta type_hierarchy --name "..."
