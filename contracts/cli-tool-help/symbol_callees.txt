chitta symbol_callees - Find all symbols that the given symbol calls (forward call graph)

Parameters:
  --name
      Symbol name to find callees for
  --id
      Symbol ID (alternative to name)
  --kind
      Filter by symbol kind when using name
  --project
      Project name to disambiguate

Example:
  chitta symbol_callees
