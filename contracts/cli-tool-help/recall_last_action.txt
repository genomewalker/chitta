chitta recall_last_action - Return last k occurrences of (tool, entity) from the CEC event tape

Parameters:
  --entity (required)

  --k

  --tool (required)


Example:
  chitta recall_last_action --tool "..." --entity "..."
