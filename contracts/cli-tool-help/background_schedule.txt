chitta background_schedule - Schedule background task

Parameters:
  --task_type (required)
      Task type to schedule
  --params
      JSON params for task

Example:
  chitta background_schedule --task_type "..."
