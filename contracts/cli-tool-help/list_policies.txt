chitta list_policies - List CEC intervention policies (shadow and active). Each entry shows rule source, kind (OpenTask/TurnInjection/GuardPolicy), shadow event count, lift, and fire count.

Parameters:
  --active_only
      If true, only return active (promoted) policies

Example:
  chitta list_policies
