chitta forget_kind - Bulk-delete all memories of a given kind (e.g. habit, unknown)

Parameters:
  --kind (required)
      Memory kind to delete
  --realm
      Optional realm filter
  --limit [default: 5000]
      Max to delete (default 5000)

Example:
  chitta forget_kind --kind "..."
