chitta forget_kind - Bulk-delete all memories of a given kind (e.g. 'habit'). Optionally filter by realm.

Parameters:
  --kind (required)
      Memory kind to delete (e.g. habit, unknown)
  --limit
      Max to delete (default 5000)
  --realm
      Optional realm filter

Example:
  chitta forget_kind --kind "..."
